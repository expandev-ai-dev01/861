import { AppRouter } from './router';

/**
 * @component App
 * @summary Root application component that renders the router
 * @type root-component
 * @category core
 */
export const App = () => {
  return <AppRouter />;
};
